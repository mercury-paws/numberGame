// takes two parameters - the player's guess and the correct number.
// It should return a string indicating whether the guess is too low,
// too high, or correct.

// export function checkGuess(guessNumber, correctNumber) {}
